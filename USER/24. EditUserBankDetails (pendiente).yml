info:
  name: 24. EditUserBankDetails (pendiente)
  type: http
  seq: 25

http:
  method: PUT
  url: "{{url}}/accreditor/EditUserBankDetails"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
