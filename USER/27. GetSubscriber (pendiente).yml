info:
  name: 27. GetSubscriber (pendiente)
  type: http
  seq: 28

http:
  method: GET
  url: "{{url}}/user/GetSubscriber"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
