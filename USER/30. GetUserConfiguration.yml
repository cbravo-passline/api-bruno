info:
  name: 30. GetUserConfiguration
  type: http
  seq: 31

http:
  method: GET
  url: "{{url}}/user/GetUserConfiguration"
  headers:
    - name: content-type
      value: application/json
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
