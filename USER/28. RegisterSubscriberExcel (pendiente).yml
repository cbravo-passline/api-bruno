info:
  name: 28. RegisterSubscriberExcel (pendiente)
  type: http
  seq: 29

http:
  method: POST
  url: "{{url}}/user/RegisterSubscriberExcel"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
