info:
  name: 3. RegisterLinkpassUrl
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/linkpass/RegisterLinkpassUrl"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: multipart-form
    data:
      - name: name
        type: text
        value: otro
      - name: name_link
        type: text
        value: https://www.uni.com
      - name: linkpassId
        type: text
        value: 9O-igwpbkkccjL_oEOCVpw@@
      - name: icon
        type: text
        value: icono43.png
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
