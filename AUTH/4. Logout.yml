info:
  name: 4. Logout
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/auth/logout"
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
