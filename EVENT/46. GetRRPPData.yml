info:
  name: 46. GetRRPPData
  type: http
  seq: 47

http:
  method: GET
  url: "{{url}}/event/GetRRPPData/31114"
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
