info:
  name: 52. GetShoppingCartVisitPurchasedStatistics
  type: http
  seq: 53

http:
  method: GET
  url: "{{url}}/event/GetShoppingCartVisitPurchasedStatistics/608e170b60b7fd6f11914a4ec9dfedbd"
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
