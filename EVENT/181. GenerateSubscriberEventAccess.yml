info:
  name: 181. GenerateSubscriberEventAccess
  type: http
  seq: 174

http:
  method: POST
  url: "{{url}}/event/GenerateSubscriberEventAccess"
  body:
    type: json
    data: |-
      {
        "event": "608e170b60b7fd6f11914a4ec9dfedbd"
      }
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
