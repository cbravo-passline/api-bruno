info:
  name: 99. GetPlanimetrySubscribersList
  type: http
  seq: 100

http:
  method: GET
  url: "{{url}}/event/GetPlanimetrySubscribersList?planimetry=hI44JEWgJ4m8TxDK7etNBQ@@"
  params:
    - name: planimetry
      value: hI44JEWgJ4m8TxDK7etNBQ@@
      type: query
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
