info:
  name: 67. GetSalesSummaryTotalsDetails
  type: http
  seq: 68

http:
  method: GET
  url: "{{url}}/event/GetSalesSummaryTotalsDetails"
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
