info:
  name: 182. SubscriberConfigSeats
  type: http
  seq: 175

http:
  method: POST
  url: "{{url}}/event/SubscriberConfigSeats"
  body:
    type: json
    data: |-
      {
        "event": "608e170b60b7fd6f11914a4ec9dfedbd"
      }
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
