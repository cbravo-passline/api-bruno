info:
  name: 17. CloseTicketSale
  type: http
  seq: 18

http:
  method: PUT
  url: "{{url}}/event/CloseTicketSale"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
