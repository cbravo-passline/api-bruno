info:
  name: 59. GetProductSalesTotalDetails
  type: http
  seq: 60

http:
  method: GET
  url: "{{url}}/event/GetProductSalesTotalDetails"
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
