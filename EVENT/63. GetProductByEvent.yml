info:
  name: 63. GetProductByEvent
  type: http
  seq: 64

http:
  method: GET
  url: "{{url}}/event/GetProductByEvent/608e170b60b7fd6f11914a4ec9dfedbd"
  body:
    type: multipart-form
    data:
      - name: id_data_base
        type: text
        value: "6"
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
