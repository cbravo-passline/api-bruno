info:
  name: 47. GetInvitationsList
  type: http
  seq: 48

http:
  method: GET
  url: "{{url}}/event/GetInvitationsList"
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
