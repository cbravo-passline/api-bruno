info:
  name: 60. GetTicketOfficeTotalDetails
  type: http
  seq: 61

http:
  method: GET
  url: "{{url}}/event/GetTicketOfficeTotalDetails"
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
