info:
  name: 76. SendCourtesyTicket
  type: http
  seq: 77

http:
  method: POST
  url: "{{url}}/event/SendCourtesyTicket"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
