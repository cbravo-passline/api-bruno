info:
  name: 83. RemoveEmailFromDataBase (pendiente)
  type: http
  seq: 84

http:
  method: DELETE
  url: "{{url}}/event/RemoveEmailFromDataBase"
  headers:
    - name: content-type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
