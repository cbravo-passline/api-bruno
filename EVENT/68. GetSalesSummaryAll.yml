info:
  name: 68. GetSalesSummaryAll
  type: http
  seq: 69

http:
  method: GET
  url: "{{url}}/event/GetSalesSummaryAll?date_end=t"
  params:
    - name: date_end
      value: t
      type: query
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
