info:
  name: 68. GetSalesSummaryAll
  type: http
  seq: 69

http:
  method: GET
  url: "{{url}}/event/GetSalesSummaryAll/608e170b60b7fd6f11914a4ec9dfedbd"
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
