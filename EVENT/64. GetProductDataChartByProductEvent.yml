info:
  name: 64. GetProductDataChartByProductEvent
  type: http
  seq: 65

http:
  method: GET
  url: "{{url}}/event/GetProductDataChartByProductEvent"
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
