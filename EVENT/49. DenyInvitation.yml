info:
  name: 49. DenyInvitation
  type: http
  seq: 50

http:
  method: POST
  url: "{{url}}/event/DenyInvitation"
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
