info:
  name: 61. GetTicketSalesByPlanimetryTotalDetails
  type: http
  seq: 62

http:
  method: GET
  url: "{{url}}/event/GetTicketSalesByPlanimetryTotalDetails"
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
