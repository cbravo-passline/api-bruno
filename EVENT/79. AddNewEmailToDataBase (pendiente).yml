info:
  name: 79. AddNewEmailToDataBase (pendiente)
  type: http
  seq: 80

http:
  method: POST
  url: "{{url}}/event/AddNewEmailToDataBase"
  headers:
    - name: content-type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
