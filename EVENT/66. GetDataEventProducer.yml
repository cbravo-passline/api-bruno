info:
  name: 66. GetDataEventProducer
  type: http
  seq: 67

http:
  method: GET
  url: "{{url}}/event/GetDataEventProducer"
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
