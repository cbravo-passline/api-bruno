info:
  name: 70. GetAccreditationUserDetails
  type: http
  seq: 71

http:
  method: GET
  url: "{{url}}/event/GetAccreditationUserDetails"
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
