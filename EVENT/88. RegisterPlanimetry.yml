info:
  name: 88. RegisterPlanimetry
  type: http
  seq: 89

http:
  method: POST
  url: "{{url}}/event/RegisterPlanimetry"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: multipart-form
    data:
      - name: name
        type: text
        value: planimetria de prueba ahora
      - name: description
        type: text
        value: esta es una prueba de planimetria
      - name: image
        type: file
        value:
          - /C:/Users/cbral/Downloads/414x497.png
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
