info:
  name: 118. GetEventListDetails(pendiente)
  type: http
  seq: 119

http:
  method: POST
  url: "{{url}}/event/GetEventListDetails"
  headers:
    - name: content-type
      value: application/json
  body:
    type: json
    data: "{\r

      \    \"eventMD5\": \"608e170b60b7fd6f11914a4ec9dfedbd\"\r

      }"
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
