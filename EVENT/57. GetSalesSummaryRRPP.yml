info:
  name: 57. GetSalesSummaryRRPP
  type: http
  seq: 58

http:
  method: GET
  url: "{{url}}/event/GetSalesSummaryRRPP"
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
