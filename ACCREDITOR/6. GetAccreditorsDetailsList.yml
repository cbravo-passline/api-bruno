info:
  name: 6. GetAccreditorsDetailsList
  type: http
  seq: 6

http:
  method: GET
  url: "{{url}}/accreditor/GetAccreditorsDetailsList"
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
