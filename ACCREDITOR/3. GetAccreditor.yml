info:
  name: 3. GetAccreditor
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/accreditor/GetAccreditor?accreditor_id=_-kIeMoAdKlKza0kSTiIVA@@"
  params:
    - name: accreditor_id
      value: _-kIeMoAdKlKza0kSTiIVA@@
      type: query
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
