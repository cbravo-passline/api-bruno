info:
  name: 83. GetTicketOfficeSales
  type: http
  seq: 84

http:
  method: GET
  url: "{{url}}/general/GetTicketOfficeSales"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
