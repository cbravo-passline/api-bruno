info:
  name: 72. GetTicketOfficeTransactions
  type: http
  seq: 73

http:
  method: GET
  url: "{{url}}/general/GetTicketOfficeTransactions?date_start=2025-11-01&date_end=&lang=es_ES"
  params:
    - name: date_start
      value: 2025-11-01
      type: query
    - name: date_end
      value: ""
      type: query
    - name: lang
      value: es_ES
      type: query
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
