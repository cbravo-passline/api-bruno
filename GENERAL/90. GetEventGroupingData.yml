info:
  name: 90. GetEventGroupingData
  type: http
  seq: 91

http:
  method: GET
  url: "{{url}}/general/GetEventGroupingData?group="
  params:
    - name: group
      value: ""
      type: query
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
