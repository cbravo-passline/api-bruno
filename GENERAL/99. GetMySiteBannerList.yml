info:
  name: 99. GetMySiteBannerList
  type: http
  seq: 96

http:
  method: GET
  url: "{{url}}/general/GetMySiteBannerList"
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
