info:
  name: 9. GetEventProducer
  type: http
  seq: 10

http:
  method: GET
  url: "{{url}}/general/GetEventProducer"
  headers:
    - name: content-type
      value: application/json
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
