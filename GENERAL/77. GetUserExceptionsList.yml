info:
  name: 77. GetUserExceptionsList
  type: http
  seq: 126

http:
  method: GET
  url: "{{url}}/general/GetUserExceptionsList?searching="
  params:
    - name: searching
      value: ""
      type: query
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
