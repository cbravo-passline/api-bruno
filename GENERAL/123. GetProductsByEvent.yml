info:
  name: 123. GetProductsByEvent
  type: http
  seq: 118

http:
  method: GET
  url: "{{url}}/general/GetProductsByEvent?event=608e170b60b7fd6f11914a4ec9dfedbd&type=products"
  params:
    - name: event
      value: 608e170b60b7fd6f11914a4ec9dfedbd
      type: query
    - name: type
      value: products
      type: query
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
