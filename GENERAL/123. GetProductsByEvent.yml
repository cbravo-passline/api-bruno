info:
  name: 123. GetProductsByEvent
  type: http
  seq: 118

http:
  method: GET
  url: "{{url}}/general/GetProductsByEvent?event=4d0ebffeac55e9e3f97252077b15c87e&type=all"
  params:
    - name: event
      value: 4d0ebffeac55e9e3f97252077b15c87e
      type: query
    - name: type
      value: all
      type: query
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
