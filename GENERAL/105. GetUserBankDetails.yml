info:
  name: 105. GetUserBankDetails
  type: http
  seq: 87

http:
  method: GET
  url: "{{url}}/general/GetUserBankDetails"
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
