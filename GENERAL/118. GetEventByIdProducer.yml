info:
  name: 118. GetEventByIdProducer
  type: http
  seq: 96

http:
  method: GET
  url: "{{url}}/general/GetEventByIdProducer"
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
