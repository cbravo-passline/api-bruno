info:
  name: 119. RemoveConsumerProduct
  type: http
  seq: 114

http:
  method: GET
  url: "{{url}}/general/RemoveConsumerProduct"
  body:
    type: text
    data: ""
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
