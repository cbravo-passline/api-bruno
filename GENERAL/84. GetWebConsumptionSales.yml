info:
  name: 84. GetWebConsumptionSales
  type: http
  seq: 84

http:
  method: GET
  url: "{{url}}/general/GetWebConsumptionSales"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
