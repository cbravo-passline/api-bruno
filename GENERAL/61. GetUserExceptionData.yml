info:
  name: 61. GetUserExceptionData
  type: http
  seq: 62

http:
  method: GET
  url: "{{url}}/general/GetUserExceptionData"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
