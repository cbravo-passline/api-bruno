info:
  name: 106. GetBankList
  type: http
  seq: 88

http:
  method: GET
  url: "{{url}}/general/GetBankList?country=chile"
  params:
    - name: country
      value: chile
      type: query
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
