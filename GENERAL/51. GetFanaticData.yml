info:
  name: 51. GetFanaticData
  type: http
  seq: 52

http:
  method: GET
  url: "{{url}}/general/GetFanaticsList"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
