info:
  name: 87. GetUserBankDetails
  type: http
  seq: 88

http:
  method: GET
  url: "{{url}}/general/GetUserBankDetails"
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
