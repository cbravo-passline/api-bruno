info:
  name: 113. RemoveProductCategory
  type: http
  seq: 108

http:
  method: DELETE
  url: "{{url}}/general/RemoveProductCategory"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
