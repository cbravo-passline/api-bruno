info:
  name: 79. EditPaypalManager
  type: http
  seq: 79

http:
  method: PUT
  url: "{{url}}/general/EditPaypalManager"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
