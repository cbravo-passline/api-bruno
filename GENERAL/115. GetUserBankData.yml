info:
  name: 115. GetUserBankData
  type: http
  seq: 94

http:
  method: GET
  url: "{{url}}/general/GetUserBankData"
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
