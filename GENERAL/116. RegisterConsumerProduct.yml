info:
  name: 116. RegisterConsumerProduct
  type: http
  seq: 111

http:
  method: POST
  url: "{{url}}/general/RegisterConsumerProduct"
  body:
    type: text
    data: ""
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
