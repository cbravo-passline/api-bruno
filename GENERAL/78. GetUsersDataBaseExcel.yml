info:
  name: 78. GetUsersDataBaseExcel
  type: http
  seq: 79

http:
  method: GET
  url: "{{url}}/general/GetUsersDataBaseExcel"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
