info:
  name: 50. GetFanaticsList
  type: http
  seq: 51

http:
  method: GET
  url: "{{url}}/general/GetFanaticsList"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
