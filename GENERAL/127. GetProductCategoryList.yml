info:
  name: 127. GetProductCategoryList
  type: http
  seq: 122

http:
  method: GET
  url: "{{url}}/general/GetProductCategoryList"
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
