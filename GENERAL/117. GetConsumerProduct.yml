info:
  name: 117. GetConsumerProduct
  type: http
  seq: 112

http:
  method: GET
  url: "{{url}}/general/GetConsumerProduct?product"
  params:
    - name: product
      value: ""
      type: query
  body:
    type: text
    data: ""
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
