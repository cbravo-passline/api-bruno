info:
  name: 97. GetUserBankData
  type: http
  seq: 95

http:
  method: GET
  url: "{{url}}/general/GetUserBankData"
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
