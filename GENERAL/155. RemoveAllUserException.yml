info:
  name: 155. RemoveAllUserException
  type: http
  seq: 127

http:
  method: DELETE
  url: "{{url}}/general/RemoveAllUserException"
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
