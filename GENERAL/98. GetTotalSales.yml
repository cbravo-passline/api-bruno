info:
  name: 98. GetTotalSales
  type: http
  seq: 80

http:
  method: GET
  url: "{{url}}/general/GetTotalSales?page=1&limit=1&lang=en&event_status=1"
  params:
    - name: page
      value: "1"
      type: query
    - name: limit
      value: "1"
      type: query
    - name: lang
      value: en
      type: query
    - name: event_status
      value: "1"
      type: query
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
