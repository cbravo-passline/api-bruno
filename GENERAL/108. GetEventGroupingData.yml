info:
  name: 108. GetEventGroupingData
  type: http
  seq: 90

http:
  method: GET
  url: "{{url}}/general/GetEventGroupingData?group="
  params:
    - name: group
      value: ""
      type: query
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
