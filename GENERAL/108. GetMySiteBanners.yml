info:
  name: 108. GetMySiteBanners
  type: http
  seq: 103

http:
  method: GET
  url: "{{url}}/general/GetMySiteBanners?producer=11266019"
  params:
    - name: producer
      value: "11266019"
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
