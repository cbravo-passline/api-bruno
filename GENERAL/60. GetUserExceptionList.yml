info:
  name: 60. GetUserExceptionList
  type: http
  seq: 61

http:
  method: GET
  url: "{{url}}/general/GetUserExceptionList"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
