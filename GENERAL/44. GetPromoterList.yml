info:
  name: 44. GetPromoterList
  type: http
  seq: 45

http:
  method: GET
  url: "{{url}}/general/GetPromoterList"
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
