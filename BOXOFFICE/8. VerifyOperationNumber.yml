info:
  name: 8. VerifyOperationNumber
  type: http
  seq: 8

http:
  method: GET
  url: "{{url}}/boxoffice/VerifyOperationNumber?operation_number=123456"
  params:
    - name: operation_number
      value: "123456"
      type: query
  auth:
    type: bearer
    token: "{{Token_boxoffice}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
