info:
  name: 7. BoxOfficeDetailTransaction
  type: http
  seq: 7

http:
  method: GET
  url: "{{url}}/boxoffice/BoxOfficeDetailTransaction?page=1&limit=100"
  params:
    - name: transaction
      value: jFLu7Ts0aHKFf1N0bUOmjQ@@
      type: query
      disabled: true
    - name: page
      value: "1"
      type: query
    - name: limit
      value: "100"
      type: query
  auth:
    type: bearer
    token: "{{Token_boxoffice}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
