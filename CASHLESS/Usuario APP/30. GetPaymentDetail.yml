info:
  name: 30. GetPaymentDetail
  type: http
  seq: 13

http:
  method: GET
  url: "{{url}}/cashless/GetPaymentDetail?order=11"
  headers:
    - name: Content-Type
      value: application/json
    - name: User-Agent
      value: Passline-PHP
    - name: Cashier-Token
      value: "{{cashier_token}}"
  params:
    - name: order
      value: "11"
      type: query
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
