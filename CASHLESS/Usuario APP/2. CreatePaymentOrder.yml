info:
  name: 2. CreatePaymentOrder
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/cashless/CreatePaymentOrder"
  body:
    type: json
    data: "{\r

      \    \"amount\":5001,\r

      \    \"payment_method\": \"mp\"\r

      }"
  auth:
    type: bearer
    token: "{{Token_user}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
