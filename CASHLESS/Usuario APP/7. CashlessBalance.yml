info:
  name: 7. CashlessBalance
  type: http
  seq: 7

http:
  method: GET
  url: "{{url}}/cashless/CashlessBalance"
  headers:
    - name: User-Agent
      value: Passline-PHP
    - name: Content-Type
      value: application/json
  auth:
    type: bearer
    token: "{{Token_user}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
