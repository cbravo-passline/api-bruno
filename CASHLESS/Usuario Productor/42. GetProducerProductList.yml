info:
  name: 42. GetProducerProductList
  type: http
  seq: 18

http:
  method: GET
  url: "{{url}}/cashless/GetProducerProductList?category=D0LMsWSJukbTMy2_1uYOGA@@"
  params:
    - name: country
      value: chile
      type: query
      disabled: true
    - name: category
      value: D0LMsWSJukbTMy2_1uYOGA@@
      type: query
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
