info:
  name: 37. RegistertProductCashlessCategory
  type: http
  seq: 13

http:
  method: POST
  url: "{{url}}/cashless/RegisterProductCashlessCategory"
  body:
    type: json
    data: "{\r\n    \"name\":\"prueba\"\r\n}"
  auth:
    type: bearer
    token: "{{Token_productor}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
